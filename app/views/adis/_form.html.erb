<%= form_for(@adi) do |f| %>
  <% if @adi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adi.errors.count, "error") %> prohibited this adi from being saved:</h2>

      <ul>
      <% @adi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
